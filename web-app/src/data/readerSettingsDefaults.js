// Cấu hình hiển thị mặc định của trình đọc (theme, cỡ chữ...) — thuần phía
// client, reading-service không có API lưu trữ cho phần này.
export const defaultReaderSettings = {
  theme: "sepia",
  fontFamily: "Merriweather",
  fontSize: 18,
  lineHeight: 1.8,
  contentWidth: "medium",
  textAlign: "left",
  autoSave: true,
};
